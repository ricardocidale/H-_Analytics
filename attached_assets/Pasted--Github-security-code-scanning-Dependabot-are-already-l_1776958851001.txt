  Github security code scanning + Dependabot are already live on the repo.                        
  Baseline is taken. 16 Dependabot alerts open (1 critical, 5 high) and 35
  CodeQL alerts open (4 critical, 25 high). Here is what to bundle into                           
  the current branch before merging Phase 3.                                                      
                                                                                                  
  ══════════════════════════════════════════════════════════════════════                          
  PASTE NOW — bundle into the current branch before merging Phase 3
  ══════════════════════════════════════════════════════════════════════                          
                                                                                                  
  A. Finish the three HIGH bugs from my previous audit message first
     (pronounSet guard, humanName empty-string schema, SPECIALIST_TOOLS                           
     boot validation). Those close Phase 3 cleanly. Do them as a single                           
     Phase 3 pt.8 commit.
                                                                                                  
  B. Then one commit: "chore(deps): patch security advisories"                                    
     Run these in order, verify app still boots between each:
                                                                                                  
       pnpm update drizzle-orm           # HIGH SQLi via bad identifier escaping
       pnpm update protobufjs            # CRITICAL arbitrary code execution
       pnpm update xlsx                  # HIGH ReDoS + prototype pollution (×2)                  
       pnpm update @types/dompurify dompurify  # 3× MED bypasses
       pnpm update hono fast-xml-parser esbuild uuid @tootallnate/once                            
                                          # rest of the Dependabot queue
                                                                                                  
     lxml and pillow are Python deps — likely vendored or transitive under
     a Python worker. Skip those; I'll flag them for a separate pass.                             
                                                        
     After the pnpm update pass:                                                                  
       - Re-run the five gates manually (tsc, lint, vocab, test:summary,
         verify:summary).                                                                         
       - Check https://github.com/Norfolk-Group/H-Analytics/security/dependabot —
         expect most alerts to auto-close within an hour of the push.                             
       - Any test that breaks from the bumps is the actual work; don't pin
         back to vulnerable versions.                                                             
                                                                                                  
  C. Also in the same commit (or a sibling): fix workflow permissions.
     Add this block to the top of every file under .github/workflows/:                            
                                                        
       permissions:
         contents: read

     Then add narrower scopes only on jobs that need them (e.g. the CI                            
     workflow that comments on PRs needs `pull-requests: write`). This
     closes CodeQL alerts #1, #2, #3 and reduces blast radius if a                                
     workflow token is stolen. Three lines per file, no design needed.                            
   
  D. One drizzle-orm verification step before the bump (important because                         
     this is your ORM, not some leaf dep):              
                                                                                                  
       grep -rn "sql.identifier\|sql.raw\|sql\`" server/ shared/ | head -40
                                                                                                  
     The CVE is specifically about raw identifier escaping. If every hit                          
     passes a hard-coded string literal (table/column name from schema,
     not user input), you're safe on the current version too. If any hit                          
     interpolates a variable into an identifier position, that callsite
     is the exploitable surface and the bump plus a review fix it.                                
   
  ══════════════════════════════════════════════════════════════════════                          
  Order of operations on your side                      
  ══════════════════════════════════════════════════════════════════════                          
   
    1. Phase 3 pt.8 — three HIGH audit fixes (previous paste).                                    
    2. One security-patch commit — Track A (dep bumps + workflow perms).
    3. Full five-gate rerun.                                                                      
    4. Merge-ready. Stop. Ping for review before pushing Phase 3 to origin.