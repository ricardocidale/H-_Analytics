You are the Finance Engineering Lead (controller + principal engineer). We are in Replit with this repo.

Mission: eliminate the need for any human to verify calculations by hand or in Excel. Implement an automated proof system that guarantees math correctness, data lineage, statement tie-outs, and reporting parity for the hospitality model (properties + management company + portfolio aggregate + consolidated with eliminations).

First, read and obey:
- .claude/claude.md
- .claude/rules/agent-persona.md
- .claude/rules/audit-doctrine.md
- /skills/finance/FINANCE_SKILL_SPECS.md
- Use the tool get_financial_standards_authority (topic="all", format="summary") before finance classification-sensitive changes.

Active Skill: Finance.VerificationAndAudit (if not defined, state the closest existing Skill and proceed with minimal additions to enable this mission).

Allowed write scope (only these paths unless you explicitly ask to expand):
- client/src/lib/**
- server/**
- .claude/rules/**
- .claude/skills/**
- package.json (only to add test scripts and dev deps)
- tests/** (create)
- test-artifacts/** (create)
Forbidden:
- client/src/components/ui/** (no UI refactors)
- client/src/pages/** (unless required to wire verification output visibility; ask first)
- migrations/**, database schema changes (ask first)

Deliverables (in this order):
0) Test Harness Setup (minimal and clean):
   - Create a tests/ folder
   - Choose ONE test runner that fits a TS monorepo style repo:
     Preferred: Vitest (fast, TS-friendly)
   - Add only the required dev dependencies and scripts in package.json:
     - "test" (runs unit + snapshot tests)
     - "test:watch"
     - "verify" (runs verification runner + tests)
   - Add a minimal vitest config (or equivalent) that supports TS/ESM.
   - Provide exact Replit shell commands to run tests and verification.

1) Audit Plan (max 1 page):
   - List the exact files/functions you will inspect first and why
   - Focus on formulas, rollups, hardcoded values, workflow correctness, reporting parity, consolidation, refinancing

2) Implement Automated Proof System with stop-the-line enforcement:
   A) Deterministic calc graph:
      - Ensure all calculations originate from assumptions → calculators → postings → statements
      - Identify and remove duplicated/ad-hoc math within allowed scope
   B) Statement tie-outs (run for every period and entity layer):
      - Balance sheet balances each period
      - Cash flow ties to change in cash each period
      - Debt roll-forward ties to debt schedule
      - Equity roll-forward ties to contributions/distributions (as modeled)
      - Intercompany matching: OpCo fee revenue equals SPV fee expense (by period)
      - Consolidated eliminations: intercompany net to zero in consolidation
      - If any tie-out fails: block outputs and emit a precise error report
   C) Hardcoded value detection:
      - Implement a checker for magic numbers in finance-critical calculation paths
      - Exclude safe constants like 12 months/year; everything else must be constants.ts or assumptions
      - Report findings and migrate as appropriate
   D) Golden scenario snapshot tests (Excel-without-Excel):
      - Create at least 5 fixture scenarios:
        1) Property cash purchase (no debt ever)
        2) Property financed purchase (LTV binding)
        3) Cash purchase → refinance in year 3 with cash-out
        4) OpCo fees + SPV fees with portfolio aggregate (no eliminations)
        5) Consolidated group with eliminations
      - Snapshot full outputs: statements + key schedules (JSON)
      - Tests fail on unintended deltas; if deltas are intentional, explain and update snapshots
   E) Reconciliation report generator (human-readable proof):
      - Generate per scenario (JSON or markdown) and write to test-artifacts/:
        - Sources & Uses at acquisition
        - NOI → FCF bridge
        - Begin Cash → End Cash bridge
        - Debt schedule reconciliation summary
        - Intercompany elimination summary
      - Ensure reports reference the same data used by statements

3) Verification runner integration:
   - Ensure client/src/lib/runVerification.ts (or equivalent) runs:
     - tie-outs
     - scenario fixtures
     - snapshots
   - Output must be UNQUALIFIED for pass
   - If verification cannot run in Replit, explain why and provide minimal reproduction steps

4) Final Output:
   - A concise “No-Excel Guarantee” checklist that is enforced by code/tests (not prose)
   - List the exact commands a developer runs to verify correctness before shipping

Rules:
- Do not guess. If a file doesn’t exist, create it in allowed scope.
- Do not silently change accounting logic. If a fix changes outputs, explain why and update snapshots intentionally.
- Smallest safe changes first.
- Remove unnecessary blank lines in any markdown you write.

Now begin:
- Step 1: confirm you read the required files (name them)
- Step 2: implement the Test Harness Setup (0)
- Step 3: present the Audit Plan (1)
- Step 4: start implementing (2–4)
