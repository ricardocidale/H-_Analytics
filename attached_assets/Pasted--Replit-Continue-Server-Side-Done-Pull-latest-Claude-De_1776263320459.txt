## Replit: Continue — Server Side Done

Pull latest. Claude Desktop pushed the remaining server items:
- `server/routes/page-visits.ts` — 4 API endpoints (GET visit, POST visit, POST save, POST analyst-run)
- Registered in `server/routes.ts`
- Cleanup added to ambient scheduler (rolling 12 months)

**Your remaining items (all client-side):**

### 1. Run DB push
```
npm run db:push
```
This creates the `user_page_visits` table from the schema Replit already built.

### 2. AgentPersonasTab.tsx
Build `client/src/components/admin/AgentPersonasTab.tsx`. Import from `shared/agent-personas.ts` (already built — exports `THE_ANALYST`, `REBECCA`, `AGENT_PERSONAS`). Two read-only cards with the persona data. Wire into `RebeccaAdminTabs` as a new tab called "AI Agents" or "Personas".

### 3. Animation Placements
- **OrbitalDots** → Replace the "Ask the Analyst" button content during research generation on `PropertyEdit.tsx` and `CompanyAssumptions.tsx`
- **NeuralGlow** → Show on `IntelligenceStatusBar.tsx` when status is "missing" (The Analyst hasn't reviewed yet). Replace the static alert icon.
- **DataFlowDots** → Show on `ResearchTheater.tsx` next to the streaming Live Output area

Import all from `@/components/ui/ai-loader`.

### 4. usePageVisit Hook
Build `client/src/hooks/usePageVisit.ts`. Calls the 4 API endpoints:
```typescript
function usePageVisit(pageKey: string, entityType?: string, entityId?: number) {
  // On mount: POST /api/page-visit/:pageKey/visit
  // Returns: { visit, isFirstVisit, isAnalystStale, recordSave, recordAnalystRun }
  // isFirstVisit = !visit.endorsed
  // isAnalystStale = !visit.lastAnalystRunAt || age > 24 hours
}
```

### 5. FirstVisitBanner Component
Build `client/src/components/intelligence/FirstVisitBanner.tsx`. Shows when `isFirstVisit` is true:
- NeuralGlow animation + glowing "Ask the Analyst" button
- Text: "The Analyst hasn't reviewed these assumptions yet"
- After Ask the Analyst completes: auto-save, set endorsed = true

### 6. Wire into Pages
Add `usePageVisit` to:
- `PropertyEdit.tsx` — pageKey: `property:{id}:edit`
- `CompanyAssumptions.tsx` — pageKey: `company:assumptions`
- Any other page with assumption inputs

Show `FirstVisitBanner` when `isFirstVisit` is true. Show glowing "Ask the Analyst" when `isAnalystStale` is true.

### 7. Vocabulary Check
Run `npx vitest run tests/audit/vocabulary-compliance.test.ts` before pushing. Forbidden terms: "Ask the Analysts" (plural), "Your analysts", "Our analysts", "the chatbot", "the assistant", "Regenerate Intelligence", "No Intelligence".

### Process
Push when done. Claude Desktop will pull and verify. Do NOT start the next batch until Claude Desktop confirms.