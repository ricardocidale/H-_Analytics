Code review found 3 important issues
Found 5 candidates, confirmed 5. See review comments for details.

Details
Severity	Count
🔴 Important	3
🟡 Nit	1
🟣 Pre-existing	0
Severity	File:Line	Issue
🔴 Important	client/src/components/admin/ai-agents/RebeccaConfig.tsx:410-425	Rebecca fallback provider can equal primary, silently defeating failover
🔴 Important	client/src/lib/specialist-nav.ts:47-54	Property-edit surfaceAnchors 'basics' and 'location' point to DOM ids that do not exist
🔴 Important	client/src/hooks/useCompanyAssumptionsForm.ts:400-408	save-tab response omits prerequisiteFailures — panel never renders on Company Assumptions
🟡 Nit	client/src/components/admin/MarketRatesTab.tsx:243-263	MarketRatesTab: refresh pending disables and relabels ALL rate rows
Annotations
 Check failure on line 425 in client/src/components/admin/ai-agents/RebeccaConfig.tsx 

@claude
claude
/ Claude Code Review
Rebecca fallback provider can equal primary, silently defeating failover

When an admin changes the Primary Provider to a value that equals the existing Fallback Provider (RebeccaConfig.tsx:411-419), the handler only updates `{ provider, model }` — it never clears `fallbackProvider`/`fallbackModel`, and the Fallback Select filter at line 476 hides the matching option from the dropdown while it persists in state. On save, the server unconditionally builds `fallback = { provider, model }` from settings (chat.ts:595-597) and on primary failure retries against the identic
 Check failure on line 54 in client/src/lib/specialist-nav.ts 

@claude
claude
/ Claude Code Review
Property-edit surfaceAnchors 'basics' and 'location' point to DOM ids that do not exist

The Fix → button on the MissingRequiredFieldsPrompt navigates to `/property/:id/edit#location` (or `#basics`), but no element in `client/src/components/property-edit/` or `PropertyEdit.tsx` carries `id="basics"` or `id="location"`, and `PropertyEdit.tsx` has no hash-reading / scroll-into-view logic. This fires live on day one: `properties.country` is nullable, and `ResearchRefreshOverlay` preflights property.risk-intelligence + property.executive-summary (both declare `lockedHard` candidateField
 Check failure on line 408 in client/src/hooks/useCompanyAssumptionsForm.ts 

@claude
claude
/ Claude Code Review
save-tab response omits prerequisiteFailures — panel never renders on Company Assumptions

The `/save-tab` handler in `server/routes/global-assumptions.ts` computes `prerequisiteFailures` at line 415 (when a toggled-on Specialist prerequisite fails) but drops the variable when serializing the response at line 488 — only `{ ok, savedTabs, verdict, requiredFieldsMissing }` is returned. Because `useCompanyAssumptionsForm.ts:408` unconditionally coerces the missing field to `[]`, `PrerequisitesFailedPanel` on `/company/assumptions` never renders even when the server has detected a failure
 Check warning on line 263 in client/src/components/admin/MarketRatesTab.tsx 

@claude
claude
/ Claude Code Review
MarketRatesTab: refresh pending disables and relabels ALL rate rows

The per-row **Analyst** button on `MarketRatesTab` uses a single shared `useRefreshRate()` mutation (line 74), and every row reads `refreshRate.isPending` for both `disabled` (line 249) and its label (line 254). Clicking Analyst on row A simultaneously disables every other row and swaps every row's label to "Studying…" — even though only row A is being fetched. The PR made this pre-existing shared-state coupling user-visible by replacing the icon-only button with a labeled one. Fix: track a per-
View more details on Claude