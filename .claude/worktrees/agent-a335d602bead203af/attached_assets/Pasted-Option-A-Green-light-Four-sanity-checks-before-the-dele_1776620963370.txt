Option A. Green light. Four sanity checks before the delete commits:
                                                                                          
  ▎ Option A confirmed. Execute with these four guardrails:                                                                           
  ▎                                                                                                                                   
  ▎ 1. Before deleting research-value-extractor.ts, grep its imports across the repo. If only the orchestrator else-branch (and T002  
  ▎ tests) import it, clean delete. If anything else — a script, a server route, an admin tool — imports it, flag and downgrade to    
  ▎ Option B for that specific caller. Do NOT delete-and-discover.                                                                    
  ▎ 2. Before deleting T002 tests, confirm they're testing retired behavior, not durable domain logic. If a test asserts "input X → 
  ▎ extractor emits Y," retired. If a test asserts "hospitality-standard fee is ~10% of GOP," durable — migrate the assertion to the  
  ▎ new path's test suite. The rule against "deleting failing tests" doesn't apply here (these pass), but the rule against "losing    
  ▎ coverage" does.                                                                                                                   
  ▎ 3. Verify the streaming-shape fix persists end-to-end. After the commit, run one live research request through the UI (or an 
  ▎ integration test if faster) and inspect assumption_guidance to confirm new-format rows land with correct fields. "Compiles" isn't 
  ▎ the bar; "DB has the right rows" is.
  ▎ 4. Re-run full five-gate suite after the commit. Watch specifically for: tests/ai/orchestrator-fallback.test.ts (fallback harness 
  ▎ I just landed), tests/proof/engine-version-drift.test.ts (will fail if you forget ENGINE_VERSION bump),                           
  ▎ tests/proof/field-definitions-no-hints.test.ts, categorical gate. If any regress, pause before push.
  ▎                                                                                                                                   
  ▎ ENGINE_VERSION bump: v2-2026-04-20-a is correct — major because the engine's emission contract changed (legacy extractor path     
  ▎ retired), even though the fingerprinted files (synthesis-schema.ts, research-prompt-builders.ts) didn't. Document that reasoning 
  ▎ in server/ai/engine-version.ts JSDoc so the v1→v2 bump isn't confusing to future readers.                                         
  ▎                                                         
  ▎ Commit message discipline:
  ▎ - Title: OT-A.4: retire legacy extractor, flip USE_AI_SDK_SYNTHESIS default on, bump ENGINE_VERSION v1→v2
  ▎ - Body: name each deletion explicitly (files, test count, else-branch lines). Anyone rolling back needs to know what was torn out.
  ▎ - Footer: Surfaces: S10, S11. Verified: TS 0, Lint 0, Vocab 11/11, test:summary PASS, Verify UNQUALIFIED. Categorical gate clean  
  ▎ on v5 data.                                                                                                                       
  ▎                                                                                                                                   
  ▎ Post-flip monitoring (already documented, restating for commit context): Sentry for ContractMigrationParityMisuseError next 72h;  
  ▎ if T1 field drifts below exemption criterion in prod, file BLOCKED and revisit.
  ▎                                                                                                                                   
  ▎ After OT-A.4 lands: immediately queue OT-A.5 scoping doc for the 6 T2 USALI anchors + mode-collapse fixes on costFB /             
  ▎ costSeg5yrPct / svcFeeGeneralMgmt / svcFeeTechRes. Budget: one $22 rerun when OT-A.5 is ready.
  ▎                                                                                                                                   
  ▎ Ship it.  