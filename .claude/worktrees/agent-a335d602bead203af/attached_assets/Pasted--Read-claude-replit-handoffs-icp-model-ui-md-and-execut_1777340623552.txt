  Read `.claude/replit-handoffs/icp-model-ui.md` and execute all three tasks in order. Commit each task separately.                                                                                                   
                                                                                                                   
  Context: The server already returns `400 { code: "ICP_MODEL_REQUIRED", models: { A, B, C } }` when the user presses the Analyst button on the Funding tab without having selected a management company model. Your  
  job is the UI that handles this.                                                                                                                                                                                    
                                                                                                                                                                                                                      
  Task A — `useAnalystRefresh.ts`: add `onIcpModelRequired` callback option to the hook; fire it when the 400 response has `code: "ICP_MODEL_REQUIRED"`.                                                              
                                                                                                                                                                                                                      
  Task B — new `<IcpModelDialog />`: three cards (A=Boutique 3–5 props, B=Growth 6–12, C=Platform 13–25). User picks one → PATCH `/api/global-assumptions` with `{ icpModelTier: "A"|"B"|"C" }` using existing
  `useUpdateAdminConfig()` → dialog closes → Analyst reruns automatically. Wire it into `CompanyAnalystOverlay.tsx` via the `onIcpModelRequired` callback from Task A. Voice: headline "The Analyst needs to know your
   scale", body "Pick the model that best fits your management company today. You can change this any time." — no "please select" / "required field" language.                                                        
                                                                                                                                                                                                                      
  Task C — pre-selection badge state: when `globalAssumptions.icpModelTier` is null, show the Analyst badge on the Funding tab in a blue/muted state with label "Select a model first" and a `Building2` Lucide icon.
  Clicking it opens the same `<IcpModelDialog />` directly.                                                                                                                                                           
                                                           
  Acceptance: pressing AnalystButton with no model → dialog opens (not a toast). Selecting Model B → verdict appears. Badge is visually distinct when no model is set.                                                
                                                                                                                                                                                                                      
  Do NOT touch any server files, shared/schema, or engine files — those are already done.