Read and execute these two handoff packets in order:                                                                                                                                                                                            
                                                      
  1. `.claude/replit-handoffs/adr-009-eww-dedup.md`                                                                                                                                                                                               
     — Remove the duplicate "Utilities" row from 3 UI components (YearlyIncomeStatement, IncomeStatementTab dashboard, and the third file named in the packet). The "Energy, Water & Waste" row added in ADR-009 Phase 1 should REPLACE the old   
  "Utilities" label, not sit beside it. Same number showing twice is a USALI violation.                                                                                                                                                           
                                                                                                                                                                                                                                                  
  2. `.claude/replit-handoffs/icp-model-ui.md`                                                                                                                                                                                                    
     — Verify all acceptance criteria are met (badge state when no ICP model selected, auto-refire after selection). IcpModelDialog.tsx already exists — check what's incomplete and finish it.
                                                                                                                                                                                                                                                  
  For each packet: execute tasks in order, commit each sub-step separately with the `Surfaces:` footer, run all five gates after each commit (npx tsc --noEmit, npm run lint, npm run test:file -- tests/audit/vocabulary-compliance.test.ts, npm 
  run test:summary, npm run verify:summary). Do not proceed to the next packet until all gates pass.                                                                                                                                              
                                                                                                       