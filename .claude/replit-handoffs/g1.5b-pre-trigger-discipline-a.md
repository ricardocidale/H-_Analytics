# G1.5b-pre-a: Server — drop save-tab Specialist dispatch + add proof test

## Doctrine Freeze Gate Check

- **Governing rule:** [`.claude/rules/analyst-trigger-discipline.md`](../rules/analyst-trigger-discipline.md), Binding 2026-04-26.
- **Gate decision:** ✅ Cleared (bugfix-against-shipped-code exception per `claude-replit-split.md`).

## Context

Removes the Specialist dispatch from `POST /api/global-assumptions/save-tab` so Save persists data only. Adds a static proof test enforcing the discipline. Parent: [`g1.5b-pre-trigger-discipline.md`](./g1.5b-pre-trigger-discipline.md).

Owner: **Replit**.

## Atomic-budget check

- **Sub-step count:** 3 (well under 7)
- **File count:** 2-3 (≤3 OK)
- **Capability domains touched:** route + verification (2 — within budget)

## Tasks

### S1: Remove Specialist dispatch from save-tab route handler

- **Files:**
  - `server/routes/global-assumptions.ts` (lines ~440-500 — the two `router.dispatch(...)` blocks for funding + revenue)
- **Change:**
  - Delete the `router.dispatch({ specialistId: MGMT_CO_FUNDING_ID, ... })` block (line ~450).
  - Delete the `router.dispatch({ specialistId: MGMT_CO_REVENUE_ID, ... })` block (line ~487).
  - Remove the local `verdict` variable assignment and any code path that returns `verdict` in the response.
  - Preserve everything else: `requireManagementAccess`, request validation (`saveTabSchema`), patch persistence, `savedTabs` union into jsonb, `requiredFieldsMissing[]` check (P6a's `findMissingRequiredFields` / `findObservedMissingCandidateFields` calls), error handling.
  - Update the response shape to:
    ```ts
    { ok: true, savedTabs: [...], requiredFieldsMissing?: string[] }
    ```
    (i.e., drop `verdict` from the response.)
  - Imports: keep `MGMT_CO_FUNDING_ID` / `MGMT_CO_REVENUE_ID` / `findMissingRequiredFields` / `findObservedMissingCandidateFields` / `RequiredFieldsMissingError` if used by required-fields enforcement; remove `createMgmtCoRouter` import if it's no longer used after dispatch removal.
- **Affected dependency surfaces:** S-Analyst-Verdict, S-Analyst-Tier0-Fallback (route surface)
- **Cross-check invariants:**
  - Per `cross-check-invariants.md`: when removing fields from a response, grep client consumers. Client code that destructured `verdict` from the save-tab response will break — this is intentional, the client packet (G1.5b-pre-b) updates the consumers. Ship (a) and (b) together or in quick succession.
  - Per `claude-replit-split.md` Guardrail #4: pre-commit gates 1–5 must pass before push.
- **Acceptance criteria:**
  - [ ] `npx tsc --noEmit` returns 0 errors.
  - [ ] `npm run lint` passes.
  - [ ] Existing route tests for save-tab still pass (or are updated to reflect new response shape — see S2).
  - [ ] `grep -n "router.dispatch" server/routes/global-assumptions.ts` returns 0 hits.
- **Test impact:** Existing save-tab tests that asserted `verdict` in response need updating in S2. The gating watchdog tests (`tests/server/global-assumptions-watchdog.test.ts` or similar) may need disabled or rewritten — record decision in commit message.
- **Rollback:** Revert the commit.

### S2: Update server-side tests for the new save-tab response

- **Files:**
  - `tests/server/global-assumptions-save-tab.test.ts` (or whatever the existing test path is — confirm at execution time)
  - Any other server-side test that asserted save-tab returned a `verdict` field
- **Change:**
  - Remove assertions on `response.body.verdict` from save-tab tests.
  - Keep assertions on `response.body.ok`, `savedTabs`, `requiredFieldsMissing`.
  - If a test exercised the watchdog flow end-to-end (Save → verdict → AnalystCheckDialog), mark it `it.skip(...)` with a comment pointing to G1.5b-pre-b (the client repurposing) and remove the skip when (b) lands.
- **Affected dependency surfaces:** Verification only.
- **Acceptance criteria:**
  - [ ] All existing save-tab integration tests PASS (or have a documented skip with TODO pointing to G1.5b-pre-b).
  - [ ] `npm run test:summary` PASS.
- **Test impact:** Updates existing tests; no new test cases.
- **Rollback:** Revert the commit.

### S3: Add proof test for trigger discipline

- **Files:**
  - `tests/proof/analyst-trigger-discipline.test.ts` (NEW)
- **Change:** Author a static-analysis test that asserts:
  1. No file matching `server/routes/**/save-tab*` (or any file with `save-tab` in the route path) imports `router.dispatch` or `consultCognitive` or any specific Specialist ID like `MGMT_CO_FUNDING_ID` for the purpose of dispatching.
  2. (Looser, optional in v1) No `useEffect` body in `client/src/hooks/**/*.ts(x)` directly calls `generateResearch` / `consultCognitive`. This is fragile to express statically — if hard, document as a manual-review item.

  Use the same pattern as existing proof tests (`tests/proof/domain-boundaries.test.ts`, `tests/proof/tool-registry.test.ts`) — read source files, regex/AST-grep for the forbidden pattern, fail with a list of offending lines.
- **Affected dependency surfaces:** Verification.
- **Acceptance criteria:**
  - [ ] Test PASSES on the post-S1 codebase.
  - [ ] Test FAILS if a developer adds `router.dispatch(...)` back into save-tab (verify by temporarily reverting S1; test should FAIL; revert the revert).
  - [ ] `npm run verify:summary` includes the new proof test and shows UNQUALIFIED PASS.
- **Test impact:** New proof test in the verification suite.
- **Rollback:** Revert the commit.

## Verification

### Gate commands (every S-step)

- [ ] `npx tsc --noEmit` — 0 TS errors
- [ ] `npm run lint` — 0 errors, 0 warnings
- [ ] `npm run test:file -- tests/audit/vocabulary-compliance.test.ts` — 11/11 PASS
- [ ] `npm run test:summary` — PASS
- [ ] `npm run verify:summary` — UNQUALIFIED PASS

### Behavioral verification (manual)

- [ ] In dev server, edit a Funding-tab field → Save. Network response is `{ ok, savedTabs, requiredFieldsMissing? }`. **No `verdict` field.**
- [ ] Browser console shows zero new errors. (The client-side dialog code may log a warning that `verdict` is missing — that's OK; G1.5b-pre-b cleans it up.)

## Surfaces footer template

Every commit emitted from this packet must end with:

```
Surfaces: S-Analyst-Verdict, S-Analyst-Tier0-Fallback
Packet: .claude/replit-handoffs/g1.5b-pre-trigger-discipline-a.md
```
