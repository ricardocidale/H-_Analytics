---
description: Show the CodeRabbit loop quick reference (commands, aliases, slash commands)
---

Run `pnpm coderabbit:help` and surface the output verbatim.

This prints a one-screen summary of:
- Toggle commands (on / off / status / help)
- Review commands (uncommitted / branch / scoped / validate:scoped)
- Natural-language aliases that work in chat
- All Claude Code `/coderabbitloop-*` slash commands
- Pointer to the full runbook

After printing, do nothing else unless the user asks a follow-up.
