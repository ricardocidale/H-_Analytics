---
description: Show the CodeRabbit loop command reference (all 6 commands)
---

Run `~/.local/share/coderabbit-loop/coderabbit-loop.sh help` and report what it printed.

The help output lists all six commands grouped by type (toggle/info vs session), the global install instructions, natural-language triggers, and the runbook path.

After reporting, ask if the user wants to start a session or has questions about a specific command.

If the helper script is not found at `~/.local/share/coderabbit-loop/`, instruct the user to run `pnpm coderabbit-loop:install` from the H+ Analytics repo first.
