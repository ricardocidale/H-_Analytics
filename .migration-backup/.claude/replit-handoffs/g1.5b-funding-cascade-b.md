# G1.5b-b: Funding tab Assumption inputs + form-hook wire

## Doctrine Freeze Gate Check

- **Governing rules:** `inflation-cascade.md`, `analyst-trigger-discipline.md`. Memory: `defaults_vs_assumptions_three_tier.md`.
- **Gate decision:** ✅ Cleared. Depends on G1.5b-a (`g1.5b-funding-cascade-a.md`) being shipped.

## Context

After G1.5b-a lands the schema columns + Defaults, this packet wires the user-facing surface: 4 inputs on the Funding tab, the form hook reads from `formData` instead of hardcoded `null`, and Save persists user assumptions to `globalAssumptions`. Parent: [`g1.5b-funding-cascade.md`](./g1.5b-funding-cascade.md).

Owner: **Replit**.

## Atomic-budget check

- **Sub-step count:** 4 (within budget)
- **File count:** 4-5 (above strict ≤3 — Funding-section + form hook + types + tests is the minimum logical unit)
- **Capability domains touched:** UI + form state + verification (within "≤2 logical domains" since this is one slice: the Assumption tier UI)

## Tasks

### S1: Add 4 inputs to FundingSection on Company Assumptions

- **Files:**
  - `client/src/components/company-assumptions/FundingSection.tsx` (or wherever the Funding tab content renders — confirm at execution time)
- **Change:** 4 new inputs grouped under a new sub-heading like "Capital Stack Discipline" (or fold into an existing "Capital Raise" group — visual fit is Replit's call):
  - **Runway buffer** — months (NumberField, suffix "months")
  - **Sizing overshoot** — percent (PctField)
  - **Revenue ramp delay** — months (NumberField)
  - **Burn flex-down** — percent (PctField)

  Each input:
  - Pre-populates from `globalAssumptions.<field>` if non-null, else falls back to the Default seeded in `model_defaults` (per the cascade rule: assumption → default).
  - On change, marks the form dirty and updates `formData` via existing `onChange` handler.
  - On Save, the value persists into `globalAssumptions` via the existing save-tab flow (which now NO LONGER dispatches the Specialist — per G1.5b-pre).

  The fifth field, `trancheGapMonths`, continues to be **computed** from `capitalRaise1Date` + `capitalRaise2Date` — no input needed; existing logic preserved.
- **Affected dependency surfaces:** S-Funding-UI
- **Cross-check invariants:**
  - Per `analyst-trigger-discipline.md`: Save must NOT auto-trigger The Analyst. Already enforced post-G1.5b-pre — verify behaviorally that adding these inputs doesn't reintroduce auto-trigger.
  - Per `branding-vocabulary-enforcement.md`: input labels follow canonical vocabulary.
  - Per `design-standards.md`: every page premium-grade — these inputs adopt the same styling as existing Funding inputs.
- **Acceptance criteria:**
  - [ ] `npx tsc --noEmit` returns 0 errors.
  - [ ] In dev server, Funding tab on Company Assumptions shows the 4 new inputs with seeded Defaults pre-populated.
  - [ ] Editing any input marks the form dirty (Save button activates).
  - [ ] Vocab test passes.
- **Test impact:** Optional new tests for the inputs.
- **Rollback:** Revert the commit.

### S2: Update `useCompanyAssumptionsForm.deriveFundingInputs()` to read from formData

- **Files:**
  - `client/src/hooks/useCompanyAssumptionsForm.ts` (lines 411-426 — the existing `deriveFundingInputs` function)
- **Change:** Replace the hardcoded `null` returns with reads from `merged.<field>`:
  ```ts
  // BEFORE
  return {
    runwayBufferMonths: null,
    sizingOvershootPct: null,
    trancheGapMonths,                  // already derived from dates
    revenueRampDelayMonths: null,
    burnFlexDownPct: null,
  };

  // AFTER
  return {
    runwayBufferMonths:     typeof merged.runwayBufferMonths     === "number" ? merged.runwayBufferMonths     : null,
    sizingOvershootPct:     typeof merged.sizingOvershootPct     === "number" ? merged.sizingOvershootPct     : null,
    trancheGapMonths,                  // unchanged — still derived from dates
    revenueRampDelayMonths: typeof merged.revenueRampDelayMonths === "number" ? merged.revenueRampDelayMonths : null,
    burnFlexDownPct:        typeof merged.burnFlexDownPct        === "number" ? merged.burnFlexDownPct        : null,
  };
  ```
  When the user clicks AnalystButton, this function feeds the Specialist's payload — now with real assumption values, not `null`. The Specialist's `requiredFields` check (per P6a `findMissingRequiredFields`) will pass when all 5 are present.
- **Affected dependency surfaces:** S-Form-Hook, S-Analyst-Verdict
- **Cross-check invariants:** Per `cross-check-invariants.md`: schema column → form hook reader. The schema column lands in G1.5b-a; this consumes it.
- **Acceptance criteria:**
  - [ ] `npx tsc --noEmit` returns 0 errors.
  - [ ] Verify in dev server: edit a value on Funding tab → click AnalystButton → DevTools Network → /api/research/generate request body's `fundingInputs` carries the user's value (not `null`).
- **Test impact:** Existing form-hook tests pass; optional new test asserting the read path.
- **Rollback:** Revert the commit.

### S3: Update Funding payload type to allow the 4 new fields

- **Files:**
  - `client/src/hooks/useCompanyAssumptionsForm.ts` OR wherever the `FundingInputs` type is declared
  - `server/routes/global-assumptions.ts` (the `saveTabSchema` already has them per G1.5b-pre's preserved `fundingInputs` block; verify)
- **Change:** Type alignment — confirm the schema-validator on the route accepts the 4 fields (already does per G1.5b-pre's preserved `saveTabSchema.fundingInputs` block which has all 5 nullable). The client-side type may need a small tweak if there's a separate `FundingInputs` interface that hardcoded `null` types.
- **Affected dependency surfaces:** S-Form-Hook
- **Acceptance criteria:**
  - [ ] `npx tsc --noEmit` returns 0 errors across server + client.
  - [ ] No `any` introduced (per `cross-check-invariants.md` Pattern 1).
- **Test impact:** TS catches drift.
- **Rollback:** Revert the commit.

### S4: Behavioral verification + tests

- **Files:**
  - Optional: `tests/integration/funding-cascade-e2e.test.ts` or extend an existing integration test to exercise Save → Reload → AnalystButton click flow
- **Change:** Manual + automated verification:
  - Manual: edit each of the 4 inputs, Save, refresh page → values persist. Click AnalystButton → /api/research/generate fires with the user's values.
  - Automated: integration test asserting the round-trip.
- **Affected dependency surfaces:** Verification
- **Acceptance criteria:**
  - [ ] All five gates green on every commit.
  - [ ] In dev server, the full cascade works end-to-end: Defaults seed admin row → admin edits → user sees new Default → user overrides on Funding tab → Saves → Specialist receives override.
- **Test impact:** Optional new integration test.
- **Rollback:** Revert the commit.

## Verification

### Gate commands (every S-step)

- [ ] `npx tsc --noEmit` — 0 TS errors
- [ ] `npm run lint` — 0 errors, 0 warnings
- [ ] `npm run test:file -- tests/audit/vocabulary-compliance.test.ts` — 11/11 PASS
- [ ] `npm run test:summary` — PASS
- [ ] `npm run verify:summary` — UNQUALIFIED PASS

### Behavioral verification (manual, post-merge)

The full cascade works end-to-end:

1. **Default tier:** Admin opens Admin > Steady State > Model Defaults > Company tab. Sees the 4 new fields with seeded values. Edits "Runway buffer" from 9 → 12 months. Saves. Refreshes — value persists.
2. **First-visit Assumption read:** A different user opens Funding tab. Their `globalAssumptions.runwayBufferMonths` is NULL → the field shows the new Default (12 months) prefilled.
3. **User override:** That user types 18 in the Runway buffer field. Save button activates. Clicks Save. Network response is `{ ok, savedTabs, requiredFieldsMissing? }` (NO `verdict` — per G1.5b-pre, save no longer dispatches Specialist). Refresh — value persists.
4. **AnalystButton flow:** User clicks AnalystButton on the Funding tab. /api/research/generate fires (the only Specialist trigger). Network request body carries `fundingInputs.runwayBufferMonths: 18`. The Specialist's `requiredFields` check passes; verdict returns with real values, not "requiredFieldsMissing".
5. Browser console: zero new errors throughout.

## Surfaces footer template

```
Surfaces: S-Funding-UI, S-Form-Hook, S-Analyst-Verdict
Packet: .claude/replit-handoffs/g1.5b-funding-cascade-b.md
```
