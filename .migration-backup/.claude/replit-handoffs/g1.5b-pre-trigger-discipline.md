# G1.5b-pre: Analyst Trigger Discipline — remove auto-trigger surfaces (Parent index)

## Doctrine Freeze Gate Check

- **Governing rule:** [`.claude/rules/analyst-trigger-discipline.md`](../rules/analyst-trigger-discipline.md) — Status: Binding (committed `e1701082`).
- **Last rule edit:** 2026-04-26.
- **Sessions stable:** 0 — but Gate is **OFF** for this packet per `claude-replit-split.md` § Doctrine Freeze Gate exception clause: this is **bug-fix-against-shipped-code** (Phase 3b's save-tab dispatch violates the rule).
- **Gate decision:** ✅ Cleared to execute under the bugfix exception.

## Context

Ricardo's binding constraint (2026-04-26): **The Analyst evaluates ONLY on explicit AnalystButton press.** Today's `/api/global-assumptions/save-tab` route dispatches `MGMT_CO_FUNDING_ID` / `MGMT_CO_REVENUE_ID` on every Save click, and two client-side hooks fire `generateResearch()` automatically (URL-param deep link + auto-refresh toggle when data goes stale). This packet removes all three.

This is a precursor to G1.5b (Defaults & Assumptions cascade for Funding fields) and G1.5c (Tier-1 deps wiring). Both downstream packets assume the trigger discipline is established.

Owner: **Replit** (UI + route handler — `claude-replit-split.md` Replit lane).

## Atomic-budget split

Two child packets, executed in dependency order:

| # | Child | Files | Sub-steps | Owner |
|---|---|---|---|---|
| 1 | [`g1.5b-pre-trigger-discipline-a.md`](./g1.5b-pre-trigger-discipline-a.md) — Server: drop save-tab Specialist dispatch + proof test | 2-3 | 3 | Replit |
| 2 | [`g1.5b-pre-trigger-discipline-b.md`](./g1.5b-pre-trigger-discipline-b.md) — Client: remove auto-trigger hooks + repurpose dialog | 4-5 | 5 | Replit |

Each child commits sub-steps independently. After (a) ships, the server response shape changes (no more `verdict` field) — (b)'s client work depends on that shape.

## Cross-cutting acceptance

After both children land:
- [ ] No call to `router.dispatch(...)`, `consultCognitive(...)`, or any Specialist evaluator lives inside a `*save-tab*` route handler or a `useEffect` body. Statically asserted by `tests/proof/analyst-trigger-discipline.test.ts` (added in child a).
- [ ] AnalystButton remains the only trigger for The Analyst. Pressing it still works end-to-end via `/api/research/generate` and friends.
- [ ] All five gates green on every commit.
- [ ] Behavioral: in dev server, edit any Funding tab field → click Save → response is data-only (no `verdict`). Click AnalystButton → /api/research/generate fires and returns The Analyst output as before.

## Out of scope (deferred)

- **Replacement save-time UX** (e.g., a non-Analyst "this value seems out of range" warning at Save). Today's auto-trigger dialog goes away; replacement is a separate packet if product wants one. Per the trigger-discipline rule, that replacement MUST be a deterministic gate, NOT a Specialist evaluation.
- **G1.5b cascade for 5 Funding fields** — separate packet, blocked-by this one.
- **G1.5c Tier-1 deps wiring** — separate packet, blocked-by this one.

## Reference

- `.claude/rules/analyst-trigger-discipline.md`
- `.claude/rules/the-analyst-persona.md`
- `.claude/rules/branding-vocabulary-enforcement.md` ("Due for review" copy, AnalystButton label)
- Memory: `analyst_trigger_discipline.md`
