# Replit — Current Tasks (April 16, 2026)

> Pull main first. 7 superseded docs were deleted. ARCHITECTURE.md is the single source of truth.

---

## WHAT CHANGED (already pushed):
1. `/company/assumptions` route: AdminRoute → ManagementRoute — user-facing, not admin-only
2. `PUT /api/global-assumptions`: requireAdmin → requireManagementAccess — users can save
3. Research endpoint: clean context gate (companyName + modelStartDate required, code: COMPANY_SETUP_INCOMPLETE)
4. Export endpoint: blocks if company name missing or properties excluded by Analyst (code: PROPERTIES_EXCLUDED)
5. analyst-watchdog.ts: can mark properties as "excluded_data" when data quality too bad
6. company-pack.ts: filters excluded properties from HMC research pool
7. properties schema: validationReason column added
8. claude.md + replit.md: Business Model section added at top

**ALL OF THE ABOVE ARE INACTIVE until you run: `npx drizzle-kit push`**

---

## DO FIRST:

### 1. Database Migration
```bash
npx drizzle-kit push
```
Creates: validation_status, last_validated_at, flagged_field_count, validation_reason columns + assumption_change_log table.

### 2. Run Seeds
```bash
npx tsx server/seeds/index.ts --force
```
The Analyst watchdog will validate all properties during seed. Check logs for flag count.

---

## THEN BUILD:

### 3. Per-Tab Save
Each of the 7 Company Assumptions tabs needs its own Save button. When user saves a tab, only that tab's fields are committed. The Analyst validates immediately after save (already wired — fires on every global-assumptions PUT).

### 4. Pulsating Analyst Button Per Tab
Every tab with assumptions/variables gets a pulsating AI icon button. User presses it for deep research on that tab's domain. Fun animation while waiting. One button per tab.

### 5. Post-Save Validation Messages
After tab save, if The Analyst finds values outside range, show inline message:
"The Analyst notes [field] at [value] is outside the expected range of [low]–[high]. Adjust or keep?"
Two buttons: "Adjust" (scrolls to field) or "Keep my value" (logged to assumption_change_log).

### 6. Handle Error Codes
- `COMPANY_SETUP_INCOMPLETE` → toast: "Company name and start date required" + button to /company/assumptions
- `PROPERTIES_EXCLUDED` → toast listing excluded properties and why

### 7. Rename "Partner Compensation" → "Management Compensation"

### 8. Fix Depreciation Years (27.5 in DB → should be 39 for US, 20 for Colombia)

---

## REFERENCE:
- Product architecture: `docs/architecture/ARCHITECTURE.md`
- Business model: `.claude/memory/project_business_model_correction.md`
- Client tasks detail: `.claude/replit-instructions/2026-04-16-adr003-client.md`
- Animation spec: `.claude/replit-instructions/2026-04-16-analyst-animation.md`
