---
name: pre-commit-gates
description: Define and enforce a mandatory five-gate verification pass that runs before every commit, in any project. Use when the project keeps shipping bugs that "should have been caught", when post-commit CI is finding what local checks should have, or when setting up engineering discipline on a new codebase.
---

# Pre-Commit Gates

A discipline for never letting a commit land that fails any check the project already runs. The gates exist; the rule is to **run them before commit, every commit, no exceptions**.

## When to use

- Project has CI that catches type errors, lint errors, test failures — but bugs still keep shipping.
- Audit sweeps regularly find 10+ issues that the existing checks would have caught locally.
- Onboarding a new agent (or human) and you need an unambiguous merge gate.
- "I'll fix it in a follow-up" has become a recurring pattern.

## When NOT to use

- Solo prototyping with no production users and no CI.
- Throwaway spikes you'll discard within the day.

## The five-gate pattern

The exact commands vary per project; the *count and shape* don't. A complete gate set covers:

| # | Gate | Common command(s) |
|---|---|---|
| 1 | **Type check** | `tsc --noEmit`, `mypy`, `pyright`, `cargo check`, `go vet` |
| 2 | **Lint** | `eslint .`, `ruff check`, `golangci-lint run`, `clippy` |
| 3 | **Domain-specific compliance** (vocabulary, security, license, copyright) | project-specific test or scanner |
| 4 | **Test suite** | `vitest run`, `pytest`, `go test ./...`, `cargo test` |
| 5 | **Invariant / proof / contract suite** | project-specific verifier that emits a binary "SAFE / NOT SAFE" verdict |

If your project has fewer than five distinct gates, write the missing ones before relying on this skill. The gate set is the contract.

## Setting it up in a new project

1. **Inventory** what already exists. Most projects have type-check + lint + tests but not the domain-compliance or invariant gates. Add those last two if the project has any business invariants worth protecting.
2. **Add a "summary" wrapper** for each command that exits 0 on pass and ≠0 on fail with a one-line summary line (e.g., `PASS 11/11 tests` or `FAIL 3 errors`). Wrappers make output diffable and CI-friendly.
3. **Document the five commands** in a single rule file (e.g., `RULES/pre-commit-gates.md` or `.cursor/rules/pre-commit-gates.md`) that every contributor reads.
4. **Add commit-message convention**: every commit message includes a verification line such as `Verified: TS 0, Lint 0, Compliance 11/11, Tests PASS, Invariants UNQUALIFIED`.
5. **Forbid `--no-verify`.** This is the single most important rule. The gate has no value if it can be bypassed.

## When a gate fails

**Stop. Do not commit.** The fix order matters:

1. **Genuine new bug** — fix the code that caused it, re-run the gate.
2. **Pre-existing failure you didn't cause** — file a `BLOCKED.md` (or a comment on whatever the active task tracker is) and escalate. Do NOT skip the gate. Do NOT delete the failing test. The failure is a real signal even if the cause predates your change.
3. **Cosmetic warning** (autogenerated file, false-positive lint) — pause anyway. The rule is "zero warnings", not "zero warnings I think matter." Either fix it, scope-suppress it with a comment explaining why, or escalate.

## Common failure modes (what this skill prevents)

- **Vocabulary regression**: a banned phrase ships in a tooltip because the compliance test wasn't run pre-commit. Caught only after deploy.
- **Contract drift hidden by `any`**: a component takes `props: any` and renders a field that no longer exists. TS would catch it if the prop were typed; the test would catch it if it ran.
- **Orphan files**: code added "in case we need it" that nothing imports. Lives in the repo unused for months until an audit finds it.
- **Schema/seed drift**: schema column added without updating the dev seed; production gets NULL where the column expects a value.

Every one of these is cheap to prevent with a 30-second local gate and expensive to find later.

## Anti-patterns

- **"I'll batch the gates and run them at the end of the session."** No — the gate is per-commit because that's the unit at which bugs become bisectable.
- **"This commit is just docs / just CSS / just config, the gates don't matter."** They matter because the next commit *will* be code, and you want a clean baseline.
- **"The pre-existing failure has been there for weeks, it's not blocking my work."** Yes it is — it's blocking the gate, which is now blocking your commit. Either fix it or file a BLOCKED.md. Don't normalize it.

## Relationship to CI

CI is the safety net, not the gate. CI catches what local gates miss; if CI is doing the catching for routine issues, the local gate isn't being enforced. A healthy ratio: CI failures should be rare (network, flaky integration tests, environment drift) — not "I forgot to run lint".

## Composition with other skills

- **`cross-check-invariants`** — what to look at *before* you run the gates, so they pass on the first try.
- **`agent-handoff-briefs`** — every handoff package should mandate the gates.
- **`ci-hygiene`** — keeps CI healthy after pulls/merges so the gate stays reliable.
