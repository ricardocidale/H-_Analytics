# screenshots/

Temporary debug and development screenshots go here, not in the repo root.

Files in this directory are gitignored — they will never be committed.
For screenshots that should be permanently referenced in the project,
use `attached_assets/` instead.
